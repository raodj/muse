#include "Core.h"
#include "ServerSession.h"
ServerSession::ServerSession(Server &server, QWidget *parent){
    Q_UNUSED(server);
    Q_UNUSED(parent);
 /*   this->server = &server;
    this->parent = parent;
    */
}
