% packages
\usepackage[utf8]{inputenc} % font encoding
\usepackage{amssymb}
%%% formatting & common needs %%%
\usepackage{newtxtext}  % times new roman
\usepackage{newtxmath}  % times new roman for math
\usepackage{fullpage}   % for using full page
\usepackage{setspace}   % for doublespace env
\usepackage{amsmath}    % for math env (delim w/ $ $)
\usepackage{wrapfig}    % wrap figure env.
\usepackage{titling}    % allows the /theauthor and /thetitle cmds
\usepackage{tikz}       % for drawing pics
    \usetikzlibrary{fit}
\usepackage[chapter]{minted}     % makes code imports pretty
\usepackage{flafter}    % place figs at or after their definition
\usepackage{setspace}   % line spacing
\usepackage{rotating}   % for sideways figures
\usepackage{pifont}
% allows for creation of list of listings when using the minted package
% a cmd for this is included in listings 
% but minted gives better syntax highlighting
\renewcommand{\listoflistings}{%
  \cleardoublepage
  %\addcontentsline{toc}{chapter}{\listoflistingscaption}%
  \listof{listing}{\listoflistingscaption}%
}

\usepackage{enumitem}
\setlist[enumerate]{itemsep=0mm} % single space enumerates

\raggedbottom   % fixes weird unwanted spacing between paragraphs
% pretty tables
\usepackage{booktabs}   % nice looking tables
\usepackage{siunitx}    % rounds numbers
\usepackage{tabularx}   % provides \multicolumn for long headers
\usepackage{multirow}


% references
\usepackage{hyperref}   % auto hyperlinks toc, list of figs, etc.
\usepackage[all]{hypcap} % fixes hyperlinks to take to image instead of caption
\usepackage[nameinlink]{cleveref}   % references and hyperlinks them (cref)
    \crefname{figure}{Figure}{Figures}    % forces capitalization of figure refs
    \crefname{section}{Section}{Sections} % forces capitalization of section refs
    \crefname{table}{Table}{Tables}       % forces capitalization of table refs
    \crefname{listing}{Listing}{Listings} % forces capitalization
\usepackage[square,numbers,sort&compress]{natbib} % modifies \cite to place citations in numerical order, also shortens (i.e. 7,8,9 -> 7-9)
\usepackage{notoccite}
% float barrier stuffs
\usepackage[section, verbose]{placeins}  % adds float barriers to sections
% adds float barriers to subsections, requires placeins package
\makeatletter
\AtBeginDocument{%
  \expandafter\renewcommand\expandafter\subsection\expandafter{%
    \expandafter\@fb@secFB\subsection
  }%
}
\makeatother

%%% end common needs %%%


% useful for macros
\usepackage{xstring}    % provides \IfEqCase for macros

% set image path, makes includes shorter to type
\graphicspath{{./images/}}

%%% special commands for title page %%%

% signature lines
\newcommand*{\Signature}[1]{%
    \par\noindent\makebox[2.5in]{\hrulefill}%
    \par\noindent\makebox[2.5in][c]{#1}%
    \\
    }

